import styled from 'styled-components';

export const Main = styled.main`
  padding: 1rem 0;

  max-width: 1280px;
  margin: 0 auto;

  > div:nth-child(1) {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  flex-direction: column;

`
