import styled from "styled-components";

export const PageNavigateStyle = styled.div`
    display: flex;
    justify-content: center;
    gap: 30px;

    button{
        cursor: pointer;
        background-color: ${props => props.theme.colors. backgroundColorBody};
    }

    margin-top: 30px;
    

` 